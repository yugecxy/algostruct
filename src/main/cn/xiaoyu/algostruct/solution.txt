class Solution{

   public boolean hasCycle(Node head){
      Node slow = head;
      Node fast = head.next;
      while(fast!=null){
        if(fast == slow) {
            return true;
        }
        fast = fast.next.next;
        slow = slow.next;
      }
      return false;

   }

}