class Solution{
  public int maxProfit(int[] prices) {
      int minVal = Integer.MaxValue;
      int result = Integer.MinValue;
      for(int price:prices){
         result = Math.max(price-minVal, result);
         minVal = Math.min(price,minVal);
      }
      return result;
  }
}